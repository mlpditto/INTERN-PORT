# ภาพรวมระบบ Internship Portfolio (V65 Full)

ระบบนี้เป็นเว็บแอปพลิเคชันสำหรับจัดการการฝึกงาน ติดตามภารกิจ (Tasks) และระบบคะแนน (Gamification) สำหรับนักศึกษาฝึกงาน โดยตัวระบบออกแบบมาให้ใช้งานผ่าน **LINE LIFF (LINE Front-end Framework)** สำหรับฝั่งผู้ใช้ และมีระบบ **Admin Dashboard** สำหรับผู้ดูแล

## 🛠 เทคโนโลยีที่ใช้ (Tech Stack)

-   **Frontend:** Vanilla HTML5, CSS3 (รองรับ Dark Mode), JavaScript
-   **Libraries:**
    -   **Font Awesome 6:** สำหรับไอคอนต่างๆ
    -   **SortableJS:** ใช้จัดการระบบลากวาง (Drag & Drop) ใน Kanban Board
    -   **jsPDF & html2canvas:** สำหรับสร้างไฟล์ใบรับรอง (PDF)
-   **Backend & Cloud Services:**
    -   **Firebase (v8):**
        -   **Firestore:** ฐานข้อมูลแบบ Real-time (Collections: `users`, `quests`, `quest_submissions`, `side_quests`, `reports`, `works`)
        -   **Firebase Auth:** สำหรับการยืนยันตัวตนฝั่ง Admin (รองรับ Magic Link)
-   **Platform Integration:**
    -   **LINE LIFF:** เชื่อมต่อกับระบบ LINE เพื่อระบุตัวตนผู้ใช้ผ่าน LINE Profile

## 📱 ระบบสำหรับผู้ใช้งาน (User Interface - `index.html`)

1.  **Dashboard ส่วนตัว:** แสดงรูปโปรไฟล์ ชื่อ และคะแนนสะสม (Score)
2.  **ติดตามสถานะฝึกงาน:** บันทึกวันเริ่ม-จบฝึกงาน และแสดงจำนวนวันที่เหลือ พร้อมปุ่มขอใบรับรองจบ (Request Cert)
3.  **ระบบภารกิจ (Kanban Board):** จัดการงานในสถานะต่างๆ (Backlog, Doing, Review, Done)
4.  **ภารกิจประจำวัน (Daily Quests):** 
    -   รับภารกิจแบบจำกัดเวลา (Timer)
    -   มีระบบ Cooldown หากทำภารกิจไม่สำเร็จ
    -   สะสมคะแนนเมื่อส่งคำตอบถูกต้อง
5.  **ส่งงานทั่วไป (Work Submission):** ฟอร์มสำหรับส่งหัวข้องานและแนบลิงก์ผลงาน
6.  **ประวัติการส่งงาน:** แสดงสถานะการตรวจสอบงานจาก Admin และคะแนนที่ได้รับ
7.  **ระบบแชท:** สามารถพูดคุยสอบถามภายในแต่ละการ์ดภารกิจได้แบบ Real-time

## 🛠 ระบบสำหรับผู้ดูแล (Admin Dashboard - `admin.html`)

1.  **จัดการงาน (Review Works):** ตรวจสอบงานที่นักศึกษาส่งมา ให้คะแนน และอัปเดตสถานะ
2.  **จัดการบอร์ดภารกิจ (Kanban Management):** ควบคุมภาพรวมของงานทั้งหมดในรูปแบบ Kanban
3.  **ตั้งค่าภารกิจประจำวัน (Daily Quest Setup):** สร้างโจทย์ กำหนดเวลา (Duration) และคะแนนสำหรับภารกิจ Daily Quest
4.  **จัดการผู้ใช้งาน (User Management):** ค้นหารายชื่อนักศึกษา ปรับแต่งคะแนน และดูประวัติรายคน
5.  **ตารางอันดับ (Leaderboard):** แสดงอันดับผู้ที่มีคะแนนสูงสุดแบบ Real-time
6.  **จัดการใบรับรอง (Certificates):** ตรวจสอบคำร้องและออกใบรับรองในรูปแบบ PDF
7.  **ความปลอดภัย:** ระบบ Login สำหรับ Admin ผ่าน Magic Link (ส่งลิงก์เข้าอีเมล)

## 📁 โครงสร้างไฟล์สำคัญ

-   `index.html`: หน้าหลักสำหรับนักศึกษา (LIFF)
-   `admin.html`: หน้าจัดการสำหรับผู้ดูแล
-   `firebase-config.js`: ไฟล์ตั้งค่าการเชื่อมต่อ Firebase
-   `style.css`: ไฟล์สไตล์กลางของระบบ
-   `firebase.json` / `.firebaserc`: ไฟล์ตั้งค่าสำหรับ Deployment บน Firebase Hosting

---
*บันทึกภาพรวมระบบโดย Antigravity AI*
