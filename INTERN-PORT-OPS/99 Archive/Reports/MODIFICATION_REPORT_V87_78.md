# Modification Report (V87.78)
## System Stabilization & Knowledge Base Enhancements

### 🚀 Core Updates
- **Project Version**: Updated to **V87.78** (Sync across all files).
- **Research Action Bar Refactor**: Transitioned from inline `onclick` string templates to a central `researchMessageStore` (JS Object).
  - **Benefit**: Resolves "Button not working" issues caused by massive AI responses, string truncation, and template literal escaping errors.
  - **Stability**: Copy, Export, and "Create Quiz" functions now handle unlimited text lengths gracefully.
- **Gemini Model ID Standardization**:
  - Refined all Gemini calls to use stable aliases (`gemini-1.5-flash` and `gemini-1.5-pro`) for the local SDK fallback.
  - Ensures 100% compatibility across Google AI for Developers and Vertex AI regions.

### 📁 Knowledge Base Fixes
- **Firestore 1MB Limit Guard**: Reduced character indexing limit to **300,000 characters**.
  - **Reasoning**: Thai characters (multi-byte) previously caused documents of ~900k characters to exceed the strict 1,048,576 byte Firestore limit.
  - **Safety**: Prevents `Error: The value of property "content" is longer than 1048487 bytes`.
- **New Feature: Delete Project**: Added a dedicated "Delete" button in the Knowledge Base Manager UI.
  - **Functionality**: Permanently removes all documents and metadata associated with a specific project/folder in a single batch.

### 🔧 DevOps & Automation
- **Turbo Deployment**: Optimized the `run_command` workflow for faster synchronization of Git and Firebase Hosting/Functions.
- **PowerShell vs Bash Utility**: Standardized command separators (using `;`) for compatibility with Windows-based PowerShell environments.

---
**Report generated by Antigravity AI on 2026-04-01.**
